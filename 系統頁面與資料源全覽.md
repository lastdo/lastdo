# 系統頁面與資料源全覽

更新日期：2026-06-27

本文件依照系統側邊欄導覽順序，整理每一頁的用途、主要操作、篩選邏輯、資料來源、快取與寫入路徑。實際導覽順序由 `render_layer/navigation.py::NAV_ITEMS` 定義。

## 0. 全系統共用架構

### 入口與共用 UI

- 主入口：`Inventory.py`
- 分頁目錄：`pages/`
- 共同導覽：`render_layer/navigation.py`
- 共同樣式：`render_layer/style.py`
- 篩選器共用元件：`render_layer/screener_common.py`
- 選股結果加入庫存/自選與走勢圖：`render_layer/watchlist.py`
- 資料診斷呈現：`render_layer/diagnostics.py`

### 導覽順序

1. 庫存股管理：`Inventory.py`
2. AI 台股趨勢分析系統：`pages/1_app_tw.py`
3. 分析記錄：`pages/2_analysis_history.py`
4. 雙龍吐珠：`pages/3_growth_screener.py`
5. 小型飆股起漲策略：`pages/7_momentum_screener.py`
6. 外資籌碼重壓選股器：`pages/4_chip_screener.py`
7. 底部剛起漲選股器：`pages/5_bottom_screener.py`
8. 雙龍吐珠策略回測：`pages/6_strategy_backtest.py`

### 共用資料來源優先順序

系統目前的策略頁大致遵循以下順序：

1. 先用官方 TWSE/TPEX 取得全市場股價、成交量、PE 或籌碼資料。
2. 再用 MOPS 月營收縮小候選池。
3. 再用官方 PE 反推近四季 EPS：`ttm_eps = close / pe_ratio_public`。
4. 最後才用 FinMind 查單檔歷史股價、均線、支撐或財報 EPS。

這樣做可以降低 FinMind 呼叫量，也避免 FinMind 限流時把「資料沒抓到」誤判成「沒有符合條件」。

### 主要資料層

| 模組 | 職責 |
|---|---|
| `data_layer/app_common.py` | 共用 runtime 設定、`analysis/` 目錄、`portfolio.json` 路徑、Streamlit secrets 讀取、FinMind URL 常數 |
| `data_layer/market_api.py` | TWSE/TPEX HTTP 抓取、TWSE `MI_INDEX` 解析、最近交易日回退 |
| `data_layer/market_data.py` | 官方資料正規化成穩定欄位：`stock_id`、`stock_name`、`market`、`close`、`vol_lot`、`rev_yoy`、`pe_ratio_public` 等 |
| `data_layer/mops_revenue.py` | MOPS 月營收，透過 `twmops.RevenueFetcher` 與 fallback HTML 解析取得上市/上櫃營收 |
| `data_layer/public_valuation.py` | TWSE/TPEX 官方 PE，並用 `attach_public_valuation()` 產生 `pe_ratio`、`ttm_eps`、`pe_label` |
| `data_layer/finmind_api.py` | FinMind `TaiwanStockPrice` 低階封裝與限流錯誤辨識 |
| `data_layer/historical_price_service.py` | 快取後的 FinMind 歷史股價與欄位清洗 |
| `data_layer/institutional_flow.py` | TWSE/TPEX 三大法人外資買賣超資料 |
| `data_layer/screener_data.py` | 選股頁共用抓取包裝：TPEX 價格、MOPS 營收、FinMind 歷史股價、FinMind 限流訊息解析 |
| `data_layer/portfolio_store.py` | 庫存/自選清單儲存，可用 Google Sheets 或本機 `portfolio.json` |
| `data_layer/export_utils.py` | CSV 匯出編碼與 dataframe 轉 bytes |
| `data_layer/data_diagnostics.py` | 資料來源成功/失敗/部分完成/FinMind 限流診斷 |
| `data_layer/contracts.py`、`data_layer/DATA_CONTRACTS.md` | 穩定資料表契約 |

### 常用官方端點

| 類型 | 來源 | 端點或 dataset |
|---|---|---|
| TWSE 最新價量 | TWSE | `https://www.twse.com.tw/exchangeReport/MI_INDEX?response=json&date={date}&type=ALLBUT0999` |
| TPEX 最新價量 | TPEX OpenAPI | `https://www.tpex.org.tw/openapi/v1/tpex_mainboard_daily_close_quotes` |
| TWSE 官方 PE | TWSE OpenAPI | `https://openapi.twse.com.tw/v1/exchangeReport/BWIBBU_ALL` |
| TPEX 官方 PE | TPEX OpenAPI | `https://www.tpex.org.tw/openapi/v1/tpex_mainboard_peratio_analysis` |
| MOPS 月營收 | MOPS/twmops | `RevenueFetcher().get_market_revenue(...)` |
| FinMind 歷史股價 | FinMind | `dataset=TaiwanStockPrice` |
| FinMind 股票資訊 | FinMind | `dataset=TaiwanStockInfo` |
| FinMind 法人買賣超 | FinMind | `dataset=TaiwanStockInstitutionalInvestorsBuySell` |
| FinMind 月營收 | FinMind | `dataset=TaiwanStockMonthRevenue` |
| FinMind 財報 | FinMind | `dataset=TaiwanStockFinancialStatements` |
| FinMind 外資持股 | FinMind | `dataset=TaiwanStockShareholding` |
| Google News | RSS | `https://news.google.com/rss/search?...` |

### 快取與限流慣例

- 大多數官方 TWSE/TPEX/MOPS 選股資料使用 `@st.cache_data(ttl=1800)`。
- 庫存頁市場快照使用 `ttl=900`。
- FinMind 歷史股價共用服務使用 `ttl=86400`。
- 選股頁遇到 FinMind `402`、`403`、`429`、`rate`、`ban`、`ip banned`，會視為資料不完整或限流，不應宣稱策略真的沒有股票。

## 1. 庫存股管理

檔案：`Inventory.py`

### 頁面目的

這是系統首頁與投資清單管理頁。它同時處理持股、自選股、即時行情覆蓋率、今日損益、未實現損益、庫存編輯、快速跳轉 AI 分析。

### 使用者操作

- 選擇或輸入 `family_id`，讓同一份 Google Sheet 或本機資料可區分不同家庭成員/投資帳本。
- 重新抓取行情，清除庫存頁快取。
- 新增股票代號，可只加自選，也可輸入成本與股數成為持股。
- 篩選持股/自選：依代號搜尋、持股/自選範圍、今日上漲/下跌、缺最新價等條件。
- 切換持股表格顯示模式。
- 修改成本與股數、刪除項目。
- 從庫存列按「AI 分析」切到 `pages/1_app_tw.py`，並把股票代號放入 `st.session_state["selected_symbol"]`。

### 資料來源

| 資料 | 來源 | 實作 |
|---|---|---|
| 庫存/自選清單 | Google Sheets 或本機 JSON | `data_layer/portfolio_store.py` |
| TWSE 行情快照 | TWSE `MI_INDEX` | `fetch_latest_twse_price_rows()` |
| TPEX 行情快照 | TPEX OpenAPI | `fetch_json_tpex(URL_TPEX_PRICE)` |
| 歷史分析索引 | 本機 `analysis/` | `load_analysis_index()` 掃描 CSV/MD |

### Google Sheets 與本機 JSON

`portfolio_store.py` 會依設定判斷是否使用 Google Sheets：

- 啟用條件：`PORTFOLIO_USE_GOOGLE_SHEETS=true`、Google Sheets spreadsheet ID、工作表名稱、service account 憑證與套件可用。
- 若未啟用或設定不足，回退到本機 `portfolio.json`。
- Google Sheets 讀取時保護股票代號字串，避免 `006208` 這類 ETF 代碼被轉成數字。
- 讀寫欄位經 `normalize_portfolio_item()` 正規化，穩定鍵包含 `row_id`、`family_id`、`stock_id`、`stock_name`、`avg_cost`、`shares`、`note`、`created_at`、`updated_at`、`is_deleted`。

### 計算邏輯

1. `fetch_market_snapshot()` 抓 TWSE/TPEX 最新價量，正規化成：
   - `stock_name`
   - `latest_price`
   - `previous_price`
   - `vol_lot`
   - `price_date`
2. `build_portfolio_rows()` 將清單與行情合併。
3. 持股計算：
   - 投入成本：`cost_price * shares`
   - 市值：`latest_price * shares`
   - 預估賣出手續費：`market_value * 0.001425`
   - 預估證交稅：`market_value * 0.003`
   - 淨未實現損益：市值 - 投入成本 - 賣出手續費 - 證交稅
   - 今日損益：`(latest_price - previous_price) * shares`
   - 部位占比：個股市值 / 全部持股市值
4. 自選股計算：
   - 今日漲跌幅：`(latest_price / previous_price - 1) * 100`
   - 成交量張數來自官方快照。
5. 首屏 dashboard 顯示最大部位、今日最佳/最差、最高自選股變動、行情覆蓋率。

### 輸出與狀態

- 主要輸出是 Streamlit dashboard、Plotly 橫條圖、持股表格、自選表格。
- 變更會寫回 Google Sheets 或 `portfolio.json`。
- 行情抓取診斷透過 `render_data_diagnostics()` 顯示。

## 2. AI 台股趨勢分析系統

檔案：`pages/1_app_tw.py`

### 頁面目的

針對單一股票做技術面、籌碼面、營收/EPS/毛利率、新聞與 AI 綜合分析。這一頁是個股深度分析頁，不是全市場選股頁。

### 使用者操作

- 輸入股票代號。
- 選擇分析期間。
- 輸入 FinMind Token。
- 輸入 Groq API Key。
- 按「分析」取得資料。
- 在兩個 tab 間切換：
  - 技術面總覽
  - AI 綜合分析
- 下載股價加法人資料 CSV。
- 儲存技術面 CSV 到 `analysis/`。
- 下載 AI 報告 Markdown。
- 儲存 AI 報告到 `analysis/`。

### 資料來源

| 資料 | 來源 | dataset / API |
|---|---|---|
| 個股歷史股價 | FinMind | `TaiwanStockPrice` |
| TAIEX 同期報酬 | FinMind | `TaiwanStockPrice`，`data_id=TAIEX` |
| 股票中文名稱 | FinMind | `TaiwanStockInfo` |
| 三大法人買賣超 | FinMind | `TaiwanStockInstitutionalInvestorsBuySell` |
| 月營收 | FinMind | `TaiwanStockMonthRevenue` |
| EPS | FinMind | `TaiwanStockFinancialStatements`，篩 `type == EPS` |
| 毛利率 | FinMind | `TaiwanStockFinancialStatements` |
| 外資持股比例 | FinMind | `TaiwanStockShareholding` |
| 近一年新聞 | Google News RSS | `news.google.com/rss/search` |
| AI 文字生成 | Groq | `llama-3.3-70b-versatile` |

### 技術指標邏輯

- 移動平均：MA5、MA20、MA60。
- RSI：預設 14 日。
- MACD：快線 12、慢線 26、signal 9。
- KD：預設 K 週期 9、平滑 3。
- 價量資料會整理成 K 線、成交量、技術指標、最近 10 筆交易資料。
- TAIEX 報酬與個股報酬會先在 Python 算好，AI prompt 直接引用，不讓模型自行估算。

### AI 報告邏輯

`generate_ai_insights()` 將以下資料組成 prompt：

- 個股基本資訊與期間報酬。
- TAIEX 同期報酬與 Alpha。
- 最新技術指標與近 60 筆精簡股價資料。
- 三大法人近 5 日合計。
- 月營收、EPS、毛利率、外資持股比例。
- Google News 近一年新聞標題與日期。

Groq 回傳 Markdown 報告，頁面可下載或儲存。若新聞抓取失敗，報告會標示新聞段落不是即時完整資料。

### 輸出與寫入

- CSV：股價、法人、技術指標合併資料。
- Markdown：AI 分析報告。
- 本機儲存位置：`analysis/`。
- CSV 編碼由 `data_layer/export_utils.py::CSV_ENCODING` 控制。

## 3. 分析記錄

檔案：`pages/2_analysis_history.py`

### 頁面目的

集中查看與管理 `pages/1_app_tw.py` 儲存下來的分析成果。

### 使用者操作

- 篩選全部、只看技術面 CSV、只看 AI 報告 MD。
- 搜尋檔名。
- 下載單一檔案。
- 預覽 CSV 或 Markdown。
- 刪除單一檔案。
- 清除全部分析記錄。

### 資料來源與邏輯

- 來源目錄：`analysis/`。
- CSV 來源：`analysis/*.csv`。
- Markdown 來源：`analysis/*.md`。
- 檔案依修改時間倒序排列。
- CSV 使用 `pd.read_csv(file, encoding=CSV_ENCODING)` 讀取。
- Markdown 使用 UTF-8 讀取。

### 寫入/刪除

此頁不產生新分析，只管理本機檔案。刪除動作會直接刪除 `analysis/` 內符合條件的 CSV/MD。

## 4. 雙龍吐珠

檔案：`pages/3_growth_screener.py`

### 頁面目的

全市場選出兩類成長股：

- 龍騰升空：成長、獲利、價量強，且價格接近均線上緣但未過度偏離。
- 潛龍在淵：成長與獲利符合，但價格接近近半年低點或尚未完全發動。

### 使用者操作

- 輸入 `family_id`，供結果加入自選股。
- 輸入 FinMind Token。
- 執行雙龍吐珠篩選。
- 清除快取與結果。
- 在三個 tab 查看：
  - 龍騰升空
  - 潛龍在淵
  - 合併清單
- 下載 CSV。
- 選擇結果股票，查看 FinMind 走勢圖與支撐/均線資訊，加入自選股。

### 策略門檻

雙龍吐珠規則與回測共用邏輯一致，主要門檻在 `backtest_common/double_dragon_rules.py::DoubleDragonThresholds`：

| 條件 | 預設值 |
|---|---|
| 收盤價 | `close > 60` |
| 成交量 | `vol_lot > 1000` 張 |
| 近兩個非二月月份平均營收 YoY | `avg_rev_yoy >= 20%` |
| 近四季 EPS | `ttm_eps >= 5` |
| 龍騰升空 PE | `pe_ratio <= 30` |
| 龍騰升空 MA60 乖離 | `close <= ma60 * 1.3` |
| 龍騰升空 MA240 乖離 | `close <= ma240 * 1.3` |
| 潛龍在淵 PE | `pe_ratio <= 20` |
| 潛龍在淵近半年低點乖離 | `close <= six_month_low * 1.2` |

### 頁面篩選流程

1. 抓 TWSE 最新股價與成交量。
2. 抓 TPEX 最新股價與成交量。
3. 抓 MOPS 最近 4 個月營收。
4. 用 `build_recent_revenue_metrics_skip_february()` 計算最近兩個非二月月份的平均營收年增率。
5. 合併價量與營收。
6. 篩：
   - `close > PRICE_MIN`
   - `vol_lot > VOL_LOT_MIN`
   - `avg_rev_yoy >= AVG_REV_YOY_MIN`
7. 抓官方 TWSE/TPEX PE。
8. `attach_public_valuation()` 產生 `pe_ratio` 與 `ttm_eps`。
9. 篩共用基本面：
   - `pe_ratio` 有值
   - `ttm_eps` 有值
   - `ttm_eps >= TTM_EPS_MIN`
   - `pe_ratio <= DRAGON_PE_MAX`
10. 對候選股用 `ThreadPoolExecutor(max_workers=3)` 查 FinMind 歷史股價。
11. 計算 MA60、MA240、近半年低點、乖離。
12. 分成龍騰升空、潛龍在淵與合併清單。

### 資料來源

| 階段 | 來源 |
|---|---|
| 最新價量 | TWSE/TPEX 官方 API |
| 月營收 | MOPS |
| 官方 PE | TWSE/TPEX 官方 PE |
| EPS | 官方 PE 反推 `close / pe_ratio_public` |
| 歷史股價/MA60/MA240/半年低點 | FinMind `TaiwanStockPrice` |
| 自選股寫入 | `portfolio_store.py`，Google Sheets 或本機 JSON |

### 診斷與輸出

- 顯示資料來源診斷。
- 顯示各策略結果數。
- 若 FinMind 限流，停止或標示不完整，不把結果當成完整零匹配。
- 可下載 CSV。
- 可加入自選股並顯示技術走勢圖。

## 5. 小型飆股起漲策略

檔案：`pages/7_momentum_screener.py`

### 頁面目的

找低中價位、營收高成長、成交量放大、EPS 為正，且價格尚未遠離年線太多的小型起漲股。

### 使用者操作

- 輸入 `family_id`。
- 輸入 FinMind Token。
- 設定最多檢查檔數。
- 執行篩選。
- 清除快取與結果。
- 下載 CSV。
- 選擇結果股票看走勢圖與 MA240，加入自選股。

### 固定策略門檻

| 條件 | 預設值 | 資料來源 |
|---|---:|---|
| 股價 | `20 <= close <= 50` | TWSE/TPEX |
| 月營收年增率 | `rev_yoy > 30%` | MOPS 最新完整月份 |
| 成交量 | `vol_lot > 300` 張 | TWSE/TPEX |
| 近四季 EPS | `ttm_eps > 1` | 官方 PE 反推 |
| 年線位置 | `close <= ma240 * 1.2` | FinMind |

### 篩選流程

1. 抓 TWSE/TPEX 最新股價成交量。
2. 抓 MOPS 最新完整月份營收。
3. 建立 `price_snapshot` 與最新營收 view。
4. 篩股價區間 20 到 50。
5. 合併營收並篩 `rev_yoy > 30`。
6. 篩成交量 `vol_lot > 300`。
7. 抓官方 PE，反推 `ttm_eps`。
8. 篩 `ttm_eps > 1`。
9. 候選股按 `rev_yoy`、`ttm_eps`、`vol_lot` 排序。
10. 以三執行緒查 FinMind 歷史股價。
11. 計算 MA240 與年線乖離。
12. 篩 `close <= ma240_limit`。
13. 結果按收盤價與營收年增排序。

### 輸出欄位

主要結果包含：

- 股票代號、股票名稱、市場
- 收盤價
- 成交量張數
- 月營收年增率
- 近四季 EPS
- PE
- 營收年月
- 年線
- 年線 1.2 倍
- 年線乖離
- 歷史交易日數

### 診斷

頁面維護 `stage_counts`，包含價量列數、營收列數、股價區間、價格+營收候選、成交量候選、EPS 候選、年線可判讀、最終結果。

## 6. 外資籌碼重壓選股器

檔案：`pages/4_chip_screener.py`

### 頁面目的

找近期外資持續買超、買超量明顯、價量與估值仍符合條件的股票。

### 使用者操作

- 輸入 `family_id`。
- 輸入 FinMind Token，主要用於結果走勢圖，不是主篩資料源。
- 設定近 N 個交易日，預設 3。
- 設定外資累積買超門檻，預設 3000 張。
- 設定 PE 上限，預設 20。
- 設定股價下限，預設 50。
- 設定成交量下限，預設 1000 張。
- 執行選股。
- 清除快取。
- 切換結果視圖：標記說明、資料表、資料來源說明。
- 下載 CSV。
- 加入自選股。

### 篩選流程

1. 抓 TWSE/TPEX 最新價量。
2. 用 `build_price_snapshot()` 正規化。
3. 篩：
   - `close > price_min`
   - `vol_lot > vol_min`
4. 從最近交易日往回掃，取得近 N 個有效交易日的 TWSE/TPEX 三大法人外資資料。
5. 將 `foreign_net_shares` 換算成張：`foreign_net_buy_lot = foreign_net_shares / 1000`。
6. 計算籌碼指標：
   - 近 N 日外資買超合計：`foreign_buy_window_lot`
   - 前 N 日外資買超合計：`foreign_buy_prev_window_lot`
   - 最近一日外資買超：`foreign_buy_latest_lot`
   - 外資連續買超天數：`foreign_buy_streak`
7. 合併價量與外資合計。
8. 篩 `foreign_net_buy_lot > foreign_buy_min`。
9. 抓官方 PE。
10. `attach_public_valuation()` 產生 PE/EPS 相關欄位。
11. 篩 `pe_ratio < pe_max`。
12. 結果以收盤價降冪排序。

### 資料來源

| 資料 | 來源 |
|---|---|
| 股價與成交量 | TWSE/TPEX 官方 API |
| 外資買賣超 | TWSE/TPEX 三大法人資料 |
| PE | TWSE/TPEX 官方 PE |
| EPS | 官方 PE 反推，可作顯示/延伸，不是目前主篩條件 |
| 結果走勢圖 | FinMind `TaiwanStockPrice` |
| 自選股寫入 | `portfolio_store.py` |

### 警示標記

頁面會用 `build_chip_alert_flags()` 標示籌碼狀態：

| 標記 | 判斷邏輯 |
|---|---|
| 外資連買 | `foreign_buy_streak >= 3` |
| 籌碼加速 | 近 N 日買超大於 0，且高於前 N 日 1.2 倍，或最近一日買超占累積買超 35% 以上 |
| 量能重壓 | 最近一日外資買超 / 成交量 >= 8% |
| 買盤降溫 | 連買至少 2 日，但近 N 日買超低於前 N 日 0.8 倍 |
| 籌碼反轉 | 累積買超大於 0，但最近一日轉賣超 |

## 7. 底部剛起漲選股器

檔案：`pages/5_bottom_screener.py`

### 頁面目的

找股價位於底部附近、營收年增為正、成交量達標、EPS 夠高，且反彈尚未過大的股票。

### 使用者操作

- 輸入 `family_id`。
- 輸入 FinMind Token。
- 設定支撐觀察月份。
- 設定最大反彈幅度。
- 設定營收年增率下限。
- 設定成交量下限。
- 設定股價下限。
- 執行選股。
- 清除快取。
- 切換標記說明/資料表/資料來源說明。
- 下載 CSV。
- 加入自選股。

### 預設與核心條件

程式側邊欄可調整門檻。頁面說明與程式邏輯包含：

- 股價：`close > price_min`
- 成交量：`vol_lot >= vol_min`
- 月營收年增：`rev_yoy > rev_growth_min`
- 官方 PE 反推 EPS：`ttm_eps > 3`
- 支撐價：最近 60 個交易日 `low` 的 15% 分位數，避免單日極端低點主導訊號。
- 反彈幅度：`(latest_close / support_price - 1) * 100`
- 最終結果會篩反彈幅度不超過使用者設定。

### 篩選流程

1. 抓 TWSE/TPEX 最新價量。
2. 抓 MOPS 近 2 個月營收。
3. 建立 `price_snapshot`。
4. 篩股價與成交量。
5. 用 `build_latest_revenue_view()` 取得最新營收。
6. 合併價量與營收，篩 `rev_yoy > rev_growth_min`。
7. 抓官方 PE，反推 `ttm_eps`。
8. 篩 `ttm_eps > 3`。
9. 以三執行緒查 FinMind 歷史股價。
10. `calc_bottom_support()`：
    - 取最近窗口歷史股價。
    - 用 low 的 15% 分位當 `support_price`。
    - 找支撐日期。
    - 計算反彈幅度。
    - 保留歷史成交量輔助判斷。
11. 篩反彈幅度。
12. 結果排序與下載。

### 警示標記

`build_alert_flags()` 會標示：

- 貼近支撐
- 起漲初段
- 空間縮小
- 營收轉弱
- 反彈無量
- 正常

### 資料來源

| 資料 | 來源 |
|---|---|
| 最新價量 | TWSE/TPEX 官方 API |
| 月營收 | MOPS |
| 官方 PE/EPS | TWSE/TPEX 官方 PE 反推 |
| 支撐與歷史股價 | FinMind `TaiwanStockPrice` |
| 自選股寫入 | `portfolio_store.py` |

## 8. 雙龍吐珠策略回測

檔案：`pages/6_strategy_backtest.py`

### 頁面目的

用指定基準日重建雙龍吐珠選股池，並計算持有期績效。這一頁是「時間點重建」與「策略績效驗證」，不是即時選股。

### 使用者操作

- 選擇選股基準日，預設為台北今日往前 92 天。
- 輸入 FinMind Token。
- 設定最多檢查候選股數，0 表示不限制。
- 選擇績效結束日，預設台北今日。
- 設定績效最多計算股票數，0 表示不限制。
- 重建基準日選股池。
- 計算持有期績效。
- 清除本頁結果。
- 若 Supabase 設定存在，可刷新快照清單、載入快照、存入 Supabase。

### 回測資料來源

| 資料 | 來源 | 實作 |
|---|---|---|
| 基準日 TWSE 歷史價量 | TWSE `MI_INDEX` 指定日期 | `backtest_data_layer/historical_prices.py` |
| 基準日 TPEX 歷史價量 | TPEX 日收盤資料 | `backtest_data_layer/historical_prices.py` |
| 基準日前完整月份營收 | MOPS | `fetch_mops_recent_revenue_frame()` |
| 單檔歷史股價 | FinMind `TaiwanStockPrice` | `backtest_data_layer/finmind_sources.py` |
| 單檔 EPS | FinMind `TaiwanStockFinancialStatements` | `fetch_financial_statement_frame()` |
| 大盤績效 | FinMind `TaiwanStockPrice`，`TAIEX` | `backtest_data_layer/performance.py` |
| 快照保存 | Supabase REST API | `backtest_data_layer/supabase_store.py` |

### 基準日選股池重建流程

1. `fetch_historical_price_snapshot(as_of_date)` 抓基準日或最近可用交易日的 TWSE/TPEX 價量。
2. 篩價量：
   - `close > 60`
   - `vol_lot > 1000`
3. 由基準日前一個完整月份開始抓 MOPS 近 4 個月營收。
4. `build_revenue_metrics_skip_february()` 取最近兩個非二月月份，計算 `avg_rev_yoy`。
5. 篩 `avg_rev_yoy >= 20`。
6. 合併價量與營收。
7. 候選股按 `avg_rev_yoy`、`stock_id` 排序，可由 `max_targets` 限制數量。
8. 用最多 3 個 worker 查每檔：
   - FinMind 歷史股價，sleep 1.2 秒。
   - FinMind 財報 EPS，sleep 2.0 秒。
9. `calc_price_metrics()` 計算：
   - MA60
   - MA240
   - 近半年低點
   - 價格對均線/低點乖離
   - 歷史交易日數
10. `calc_ttm_eps()` 取基準日前最近 4 季 EPS 合計。
11. 用 `pe_ratio = close / ttm_eps` 估 PE。
12. `apply_double_dragon_flags()` 套用雙龍規則。
13. 僅保留 `is_common_pass` 的股票，並標示分支：
   - 龍騰升空
   - 潛龍在淵
   - 兩者皆符合

### 持有期績效

`backtest_data_layer/performance.py::build_return_analysis()`：

1. 從快照中取雙龍符合的股票。
2. 以基準日收盤價作為進場價。
3. 逐檔抓基準日至績效結束日前的可用 FinMind 股價。
4. 用最後可用收盤價作為出場價。
5. 計算個股報酬。
6. 抓 TAIEX 同期報酬。
7. 建立等權投組曲線。
8. 計算 KPI：
   - 投組報酬
   - 大盤報酬
   - 超額報酬
   - 上漲檔數/下跌檔數
   - 已定價檔數
   - 未定價檔數

### Supabase 快照

`backtest_data_layer/supabase_store.py` 會從 Streamlit secrets 讀：

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY` 或 `SUPABASE_ANON_KEY`

資料表：

- `backtest_runs`
- `backtest_snapshot_rows`

若未設定 Supabase，頁面只顯示提示，不影響本機回測。

### 輸出

- Tab：
  - 龍騰升空
  - 潛龍在淵
  - 雙龍合併
  - 基準日明細
  - 持有期績效
- 基準日明細可包含稽核欄位。
- 快照與績效都可下載 CSV。
- 績效圖由 Plotly 顯示投組、大盤、超額報酬曲線。

## 9. 跨頁寫入與匯出整理

| 功能 | 寫入位置 | 觸發頁 |
|---|---|---|
| 庫存/自選新增、修改、刪除 | Google Sheets 或 `portfolio.json` | 庫存頁、各選股頁的加入自選 |
| 技術面 CSV 儲存 | `analysis/*.csv` | AI 台股趨勢分析 |
| AI 報告 MD 儲存 | `analysis/*.md` | AI 台股趨勢分析 |
| 選股結果 CSV 下載 | 瀏覽器下載，不預設寫入 repo | 雙龍、小型飆股、外資籌碼、底部、回測 |
| 回測快照保存 | Supabase | 策略回測 |

## 10. 重要資料欄位契約

| 欄位 | 意義 |
|---|---|
| `stock_id` | 股票代號，用於 join |
| `stock_name` | 股票名稱 |
| `market` | `TWSE` 或 `TPEX` |
| `close` | 選股頁使用的官方收盤價 |
| `latest_price` | 庫存頁行情快照使用的最新價 |
| `price_date` | 行情來源日期 |
| `vol_lot` | 成交量張數 |
| `rev_ym` | ROC 年月 |
| `rev_yoy` | 月營收年增率 |
| `avg_rev_yoy` | 最近 N 個月營收年增率平均 |
| `pe_ratio_public` | 官方 PE 原始值 |
| `pe_ratio` | 策略使用 PE，通常等於官方 PE 或由 EPS 推算 |
| `ttm_eps` | 近四季 EPS，選股頁多由官方 PE 反推，回測頁由 FinMind EPS 合計 |
| `foreign_net_shares` | 外資買賣超股數 |
| `foreign_net_buy_lot` | 外資買賣超張數 |

## 11. 已知設計取捨

- 即時選股頁優先用官方 TWSE/TPEX/MOPS，FinMind 只放在歷史股價或補充資料階段。
- 官方 PE 反推 EPS 是為了快速、免費、低 API 成本地做初篩；它不是完整財報 EPS 定義。
- 回測頁為了重建基準日狀態，會改用 FinMind 財報 EPS 計算 TTM EPS，與即時頁的官方 PE 反推 EPS 不完全相同。
- 選股頁會顯示資料來源與階段數，避免黑箱篩選。
- FinMind 限流是資料品質事件，不應被當作策略零結果。
- 庫存儲存後端可切換，Google Sheets 不可用時會回退或顯示連線錯誤；本機路徑是 `portfolio.json`。
