# 資料源整理

本文件整理目前專案可用的台股資料源、各策略實際使用的來源，以及最近研究的 MOPS 損益表取得方式。  
重點原則：能用官方 TWSE/TPEX/MOPS 的資料就先用官方資料；FinMind 留給歷史股價、均線、支撐線、個股細節資料等官方批次來源不足的地方。

## 目前可用資料源總表

| 資料類型 | 主要來源 | 目前程式位置 | 用途 | 備註 |
|---|---|---|---|---|
| 上市最新股價/成交量 | TWSE MI_INDEX | `data_layer/market_api.py::fetch_latest_twse_price_rows` | 選股第一層價格、成交量篩選；庫存市值估算 | 會回看近 7 天交易日，解析 `MI_INDEX` 表格 |
| 上櫃最新股價/成交量 | TPEX OpenAPI | `https://www.tpex.org.tw/openapi/v1/tpex_mainboard_daily_close_quotes` | 選股第一層價格、成交量篩選；庫存市值估算 | 正規化成 `stock_id`, `stock_name`, `close`, `vol_lot` |
| 官方本益比 | TWSE/TPEX OpenAPI | `data_layer/public_valuation.py` | PE 篩選、用 PE 反推近四季 EPS | 優先用官方 PE，避免為 EPS 大量打 FinMind |
| 月營收 | MOPS via `twmops` | `data_layer/mops_revenue.py` | 成長股、低檔反彈股的營收成長篩選 | 使用 ROC 年月，抓上市/上櫃、一般/異業公司類型 |
| 三大法人/外資買賣超 | TWSE T86、TPEX OpenAPI/舊 API | `data_layer/institutional_flow.py` | 籌碼策略的外資連買與 N 日買超 | 目前主要正規化外資相關淨買賣股數 |
| 歷史股價 | FinMind `TaiwanStockPrice` | `data_layer/finmind_api.py` | MA60、支撐線、歷史圖表 | 容易限流，應放在最後階段、縮小候選名單後再查 |
| 個股分析舊頁資料 | FinMind 多個 dataset | `pages/1_app_tw.py` | 個股分析頁：股價、TAIEX、月營收、EPS、毛利率、持股等 | 這是單股分析用途，不適合拿來全市場掃描 |
| MOPS 損益表 | `twmops.FinancialFetcher`、MOPSFin compare API | `data_layer/mops_financial.py` | 財報研究、單股/少量股票財務指標 | 毛利率選股已不作為主策略，但來源可保留備查 |
| 庫存/自選股資料 | Google Sheets 或本機 JSON | `data_layer/portfolio_store.py` | 庫存與自選股清單 | Google Sheets 需要 service account 設定 |

## 正規化後常用欄位

| 欄位 | 意義 | 主要來源 |
|---|---|---|
| `stock_id` | 股票代碼 | 各來源正規化後統一欄位 |
| `stock_name` | 股票名稱 | TWSE/TPEX 股價資料 |
| `market` | `TWSE` 或 `TPEX` | 股價資料 |
| `close` | 最新收盤價 | TWSE/TPEX 股價資料 |
| `vol_lot` | 成交量，單位張 | TWSE/TPEX 股價資料，原始股數除以 1000 |
| `rev_ym` | 營收年月，ROC yyyMM | MOPS 月營收 |
| `rev_yoy` | 單月營收年增率 | MOPS 月營收 |
| `avg_rev_yoy` | 近 N 月平均營收年增率 | `build_recent_revenue_metrics` |
| `pe_ratio_public` | 官方本益比 | TWSE/TPEX 官方 PE |
| `pe_ratio` | 策略使用的 PE | 目前等同官方 PE |
| `ttm_eps` | 近四季 EPS 估算 | `close / pe_ratio_public` 反推 |
| `foreign_net_shares` | 外資相關淨買賣股數 | TWSE/TPEX 法人資料 |

## 各策略目前使用的資料源

### 成長股策略

程式：`pages/3_growth_screener.py`

取得流程：

1. 先抓 TWSE/TPEX 最新股價與成交量。
2. 用 MOPS 月營收抓最近 3 個月資料。
3. 用最近 2 個月營收年增率計算 `avg_rev_yoy`、`latest_rev_yoy`、`prev_rev_yoy`。
4. 合併價格、成交量、營收，先篩掉不符合成交量、股價、營收成長的股票。
5. 抓 TWSE/TPEX 官方本益比，合併後用 `pe_ratio` 篩 PE。
6. 最後才用 FinMind `TaiwanStockPrice` 查 MA60，檢查股價是否離季線太遠。

這個策略的核心來源是官方股價、MOPS 月營收、官方 PE；FinMind 只負責最後的歷史均線。

### 籌碼策略

程式：`pages/4_chip_screener.py`

取得流程：

1. 抓 TWSE/TPEX 最新股價與成交量。
2. 依使用者設定的 N 日，逐日抓 TWSE/TPEX 法人資料。
3. 計算外資 N 日買超、前一段 N 日買超、最新一日買超、連買天數。
4. 合併價格與法人資料後，先用股價、成交量、外資買超做候選名單。
5. 再抓官方 PE，篩本益比。
6. watchlist 小圖才用 FinMind 歷史股價載入近 120 日線圖。

這個策略不需要 MOPS 損益表；主要是 TWSE/TPEX 股價、法人資料、官方 PE。

### 低檔反彈策略

程式：`pages/5_bottom_screener.py`

取得流程：

1. 抓 TWSE/TPEX 最新股價與成交量。
2. 抓 MOPS 最近 2 個月月營收，取最新月營收年增率。
3. 用股價、成交量、營收年增率先縮小候選名單。
4. 抓官方 PE，反推 `ttm_eps = close / pe_ratio_public`，篩近四季 EPS。
5. 最後用 FinMind `TaiwanStockPrice` 查近 N 個月歷史股價。
6. 用歷史低點分位數計算支撐價與反彈幅度。

這個策略最吃 FinMind 呼叫量，所以前面的官方資料篩選很重要。FinMind 若遇到 402/403/429 或 `ip banned`，不能把結果當作「沒有股票符合」，而是資料不完整。

### 產品組合/毛利率策略

程式：`pages/6_product_mix_screener.py`、`data_layer/mops_financial.py`

你已決定不要這個毛利率選股。它目前可以視為「研究用/備查」，不建議當主策略入口。

它原本使用：

1. TWSE/TPEX 最新股價與成交量。
2. TWSE/TPEX 營益分析 OpenAPI：
   - TWSE: `https://openapi.twse.com.tw/v1/opendata/t187ap17_L`
   - TPEX: `https://www.tpex.org.tw/openapi/v1/mopsfin_187ap17_O`
3. MOPSFin compare API 查毛利率序列：
   - `https://mopsfin.twse.com.tw/compare/data`

保留價值：MOPS 損益表取得方式、季度判斷、毛利率/淨利率計算邏輯仍可供未來單股分析或財報研究使用。  
不再作為主策略的原因：毛利率連三季改善太容易變成產業結構與會計分類差異的掃描，實戰上需要更多質化判斷，不適合單獨做全市場選股。

## MOPS 月營收取得方式

程式：`data_layer/mops_revenue.py`

使用套件：

```python
from twmops import RevenueFetcher
```

呼叫方式：

```python
RevenueFetcher().get_market_revenue(roc_year, month, market, company_type)
```

目前參數：

| 參數 | 值 | 說明 |
|---|---|---|
| `roc_year` | 民國年，例如 115 | MOPS 使用 ROC 年 |
| `month` | 1-12 | 營收月份 |
| `market` | `sii`, `otc` | 上市、上櫃 |
| `company_type` | `0`, `1` | 一般/異業類型，目前兩種都抓 |

正規化輸出：

| 欄位 | 意義 |
|---|---|
| `stock_id` | 股票代碼 |
| `stock_name_mops` | MOPS 公司名稱 |
| `rev_ym` | ROC 年月 |
| `rev_yoy` | 年增率 |
| `rev_cur` | 當月營收 |
| `rev_ly` | 去年同月營收 |
| `mops_market` | MOPS 市場別 |
| `mops_company_type` | MOPS 公司類型 |
| `mops_comment` | MOPS 備註 |

## MOPS 損益表取得方式

程式：`data_layer/mops_financial.py`

### 方式一：`twmops.FinancialFetcher`

使用套件：

```python
from twmops import FinancialFetcher
```

呼叫方式：

```python
FinancialFetcher().get_simplified_statement(
    stock_id,
    roc_year,
    quarter,
    "income_statement",
)
```

目前用來讀的損益表項目：

| 指標 | 優先取用項目 |
|---|---|
| 營業收入 | `Revenue` |
| 營業毛利 | `GrossProfit`，備援 `GrossProfitLossFromOperations` |
| 本期淨利 | `ProfitLoss`，備援 `IncomeAfterTaxes`、`ProfitLossAttributableToOwnersOfParent` |

計算方式：

```text
毛利率 = 營業毛利 / 營業收入 * 100
淨利率 = 本期淨利 / 營業收入 * 100
```

重要注意：

- MOPS 第 4 季常見是全年累計數，程式目前用 Q4 - Q1 - Q2 - Q3 還原單季。
- 最近完整財報季度用 `latest_completed_financial_quarter()` 判斷：
  - 5/16 之後可用 Q1
  - 8/15 之後可用 Q2
  - 11/15 之後可用 Q3
  - 4/1 之後可用前一年 Q4
  - 4/1 之前先用前一年 Q3
- 這個來源適合單股或少量股票查詢，不適合無限制全市場逐檔打。

### 方式二：MOPSFin compare API

用途：批次查多檔股票的毛利率季度序列。

入口：

```text
GET  https://mopsfin.twse.com.tw/
POST https://mopsfin.twse.com.tw/compare/data
```

目前 payload 重點：

| 欄位 | 值 | 說明 |
|---|---|---|
| `compareItem` | `GrossMargin` | 查毛利率 |
| `ylabel` | `%` | 圖表單位 |
| `year` | 西元年 | 例如 ROC 115 年會送 2026 |
| `season` | 季度 | 1-4 |
| `quarter` | `true` | 用季度資料 |
| `revenue` | `false` | 不查營收圖 |
| `companyAvg` | `false` | 不查公司平均 |
| `bcodeAvg` | `false` | 不查產業平均 |
| `companyId` | 股票代碼，可重複多筆 | 批次公司清單 |

回傳重點：

- `xaxisList`：季度序列，例如 `2026Q1`
- `showNameList` / `displayCompanyId`：公司名稱或代碼顯示
- `graphData`：每家公司對應的毛利率序列

目前用法是找最新季、前一季、前二季三個點，判斷是否 `latest > prev > third`。  
因為你已不採用毛利率選股，這段只列為資料研究來源，不建議繼續放在主選股流程。

## 官方 PE 與 EPS 反推

程式：`data_layer/public_valuation.py`

來源：

```text
TWSE https://openapi.twse.com.tw/v1/exchangeReport/BWIBBU_ALL
TPEX https://www.tpex.org.tw/openapi/v1/tpex_mainboard_peratio_analysis
```

使用方式：

1. 抓官方 PE。
2. 正規化成 `stock_id`, `pe_ratio_public`。
3. 合併到候選股。
4. 用：

```text
ttm_eps = close / pe_ratio_public
```

這個 EPS 是用官方 PE 反推的近四季 EPS 估算，不是逐季財報 EPS 明細。優點是快、官方、適合全市場篩選；缺點是遇到無 PE、虧損、資料缺漏時不能使用。

## FinMind 使用邊界

目前共用入口：

```text
https://api.finmindtrade.com/api/v4/data
```

目前主要使用 dataset：

| dataset | 用途 |
|---|---|
| `TaiwanStockPrice` | 歷史股價、MA60、支撐線、圖表 |
| `TaiwanStockInfo` | 單股分析頁查股票名稱 |
| `TaiwanStockInstitutionalInvestorsBuySell` | 舊單股分析頁三大法人 |
| `TaiwanStockMonthRevenue` | 舊單股分析頁月營收 |
| `TaiwanStockFinancialStatements` | 舊單股分析頁 EPS、毛利率 |
| `TaiwanStockShareholding` | 舊單股分析頁外資持股比例 |

策略頁原則：

- 全市場第一層不要先打 FinMind。
- 先用 TWSE/TPEX/MOPS 批次資料縮小名單。
- FinMind 放最後查歷史股價、MA60、支撐線。
- 遇到 402/403/429、`rate`、`ban`、`ip banned`，要視為資料不完整，不可視為策略沒有符合股票。

## 建議後續整理方向

1. 毛利率選股頁如果確定不要，可以從導覽移除，或把 `pages/6_product_mix_screener.py` 移到 `archive/legacy/pages/`。
2. `data_layer/mops_financial.py` 可以保留，但定位改成財報研究 helper，不作為主選股入口。
3. 策略資料源優先順序維持：
   - 第一層：TWSE/TPEX 股價、成交量、官方 PE、MOPS 月營收、法人資料。
   - 第二層：官方 PE 反推 EPS。
   - 最後一層：FinMind 歷史股價。
   - 研究/補充層：MOPS 損益表。
